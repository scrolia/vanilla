{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^7.3.0",
        "vite-tsconfig-paths": "^6.0.1"
    },
    "dependencies": {
        "@scrolia/vanilla": "workspace:~"
    }
}
