{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^8.2.2",
        "vite-tsconfig-paths": "^6.1.1"
    },
    "dependencies": {
        "@scrolia/vanilla": "workspace:~"
    }
}
