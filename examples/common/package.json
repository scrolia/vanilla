{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^7.2.7",
        "vite-tsconfig-paths": "^6.0.0"
    },
    "dependencies": {
        "@scrolia/vanilla": "workspace:~"
    }
}
