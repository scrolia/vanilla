{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^7.3.1",
        "vite-tsconfig-paths": "^6.1.0"
    },
    "dependencies": {
        "@scrolia/vanilla": "workspace:~"
    }
}
