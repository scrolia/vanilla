{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^7.1.0",
        "vite-tsconfig-paths": "^5.1.4"
    },
    "dependencies": {
        "@scrolia/vanilla": "workspace:~"
    }
}
